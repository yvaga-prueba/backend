---
apiVersion: v1
kind: Service
metadata:
  name: mariadb-host
spec:
  ports:
    - port: 3306
---
apiVersion: v1
kind: Endpoints
metadata:
  name: mariadb-host
subsets:
  - addresses:
      - ip: 187.127.3.252 # <--- CAMBIA ESTO por la IP PRIVADA de tu servidor (no la pública) property inet cuando se ejecuta 'ip addr show'
    ports:
      - port: 3306